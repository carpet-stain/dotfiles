# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# git-flow base: direnv local overrides (credential scoping). Kept here because
# this overlay's .gitignore replaces the base's on layering — copier can't merge
# text files across templates.
.envrc.local
