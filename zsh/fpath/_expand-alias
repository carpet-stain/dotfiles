#!/usr/bin/env zsh

_expand-alias () {
    zle _expand_alias
    # A leading backslash suppresses expansion (see keybindings.zsh) —
    # strip it so the command runs clean
    [[ $LBUFFER == "\\"* ]] && LBUFFER=${LBUFFER:1}
    zle self-insert
}
