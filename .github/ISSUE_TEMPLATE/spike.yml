name: Spike / research
description: A question with a concrete deliverable
title: "spike(<scope>): "
labels: ["spike"]
body:
  # <scope> = this repo's scope table (AGENTS.md): zsh, zellij, git, nvim, macos, theme, python.
  - type: textarea
    id: question
    attributes:
      label: Question
      description: The one thing this spike answers. Never open-ended.
    validations:
      required: true
  - type: textarea
    id: outcome
    attributes:
      label: Expected outcome
      description: The concrete artifact this ends with — a decision, an ADR, a follow-up issue.
    validations:
      required: false
